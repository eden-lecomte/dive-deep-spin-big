// ModesPanel is no longer needed - voting and presentation modes are now in the header
// This file is kept for backward compatibility but can be removed if not used elsewhere
export default function ModesPanel() {
  return null;
}
